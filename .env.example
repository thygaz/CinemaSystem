# Configurações da Aplicação
APP_HOST=localhost
APP_PORT=8080

# Configurações do Banco de Dados PostgreSQL (rodando via Docker Compose)
# NOTA: DB_HOST deve ser o nome do serviço no seu docker-compose.yml (geralmente 'db' ou 'postgres')
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=SUA_SENHA_SEGURA  # ALTERE ESTE VALOR ANTES DE USAR!
DB_NAME=cinemasystem2_db
